"use strict";
const mediaConfig = {
  /*
  |--------------------------------------------------------------------------
  | Media config
  |--------------------------------------------------------------------------
  */

  main_upload_dir: "/public/uploads",

  imageUpload: {
    ImagePath: "certificate/",
  },
};

module.exports = mediaConfig;
